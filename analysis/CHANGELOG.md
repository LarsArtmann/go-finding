hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

## [1.13.0] - 2026-09-22

Version-alignment release with core v1.13.0. No analysis-module code
hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

hanges; the only delta since [1.12.0] is the `ginkgo` test-dependency bump.

