# docs/planning

Execution plans (Pareto breakdowns, dependency graphs, tier tables).
A plan is a snapshot of intent at creation time and is never rewritten:
progress is recorded as dated annotations in its annotation log, and
completed plans stay as historical record. Live priorities live in the
root `TODO_LIST.md`; long-term direction in `ROADMAP.md`.
