# docs/reviews

Point-in-time review and audit reports (code reviews, architecture
assessments, external-tool evaluations). They describe the tree as it
was when written and are annotated in place as findings get resolved;
resolved reports move to `archived/`. They are records, not living
docs — do not treat a finding as still-open without checking the code.
